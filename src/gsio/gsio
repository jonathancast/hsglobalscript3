#! /usr/local/hsglobalscript/bin/gsi

gsmain
    (λ 'pos. λ 'ty. type-checker.unify pos ty (type.app (type.const (gsvar qq{gsio.m})) type.empty-signature))
    (λ 'prog. λ 'args. undefined >>=_gsi.monad λ 'td. undefined)
